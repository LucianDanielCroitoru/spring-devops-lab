apiVersion: v1
kind: Service
metadata:
  name: spring-devops-lab-app
spec:
  selector:
    app: spring-devops-lab-app
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort